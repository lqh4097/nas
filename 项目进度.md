# 🐦 鸟鸣 NAS 项目进度

> 自动生成 2026-06-20 17:20　｜　刷新：`python src/project_status.py`

---

## 📊 进度总览

| 阶段 | 状态 | 说明 |
|------|------|------|
| ① 环境 & 数据 | ✅ | val/test 已划分、stats 已算 |
| ② 模型基线 | 4/7 | 核心 4/4，选做 0/3 |
| ③ NAS 主搜索 (full) | ✅ | Pareto 18 个，待复训 |
| ④ 搜索方法基线 | 0/2 | 随机搜索 0 seed，标准NSGA-II ⬜ |
| ⑤ Pareto 复训（真实指标）| ⬜ | 未运行 |
| ⑥ 消融实验 (RQ3) | 1/25 | 5 组 × 5 seed |

---

## ① 模型基线（完整训练，真实 Val Acc）

> 任务准确率饱和（基线普遍 97–98%），对比重点在**等准确率下的参数量/延迟**。

| 类别 | 模型 | 参数量(M) | Val Acc | 状态 |
|------|------|-----------|---------|------|
| 核心 | Manual-CNN | 2.508 | **0.9839** | ✅ |
| 核心 | MobileNetV2 0.5× | 0.739 | **0.9806** | ✅ |
| 核心 | ShuffleNetV2 0.5× | 0.383 | **0.9787** | ✅ |
| 核心 | MobileNetV3-Small | 1.559 | **0.9815** | ✅ |
| 选做 | Manual-CRNN | 0.134 | — | ⬜ 未训练 |
| 选做 | GhostNet 0.5× | 1.358 | — | ⬜ 未训练 |
| 选做 | EfficientNet-Lite0 | 3.422 | — | ⬜ 未训练 |

## ② NAS 主搜索（full：NSGA-II + EDA + Surrogate）

- 最终 Pareto 前沿：**18** 个架构（已去重）
- 最高 proxy acc：0.7990（0.4665M）
- 最小架构：0.0174M（proxy acc 0.2842）
- ⚠️ 上述为 proxy 估计值，仅用于排序；真实指标见 ④ 复训

## ③ Pareto 架构复训（真实 Test Acc）

- ⬜ 未运行（先跑 `nsga2_eda.py`，再 `retrain_pareto.py`）

## ④ 搜索方法基线 & 消融实验

**随机搜索（Random Search，多 seed）**

- ⬜ 未运行

**消融实验组（RQ3 留一法；`baseline` 组即标准 NSGA-II）**

| 组 | seed | gen | 真实评估 | Pareto | 状态 |
|----|------|-----|---------|--------|------|
| full | seed42 | 15 | 65 | 18 | ✅ |

---

## ⑤ 数据产物 & 代码清单（参考）

**数据产物**

- `stats.json`：mean=[0.197, 0.485, 0.502] std=[0.090, 0.212, 0.079]（抽样 5000）
- `split_index.json`：val 3294 / test 3318（seed 42）

**代码文件**

| 文件 | 角色 |
|------|------|
| `baseline_classic.py` | 基线：ShuffleNet/MobileNetV3/GhostNet/EffLite |
| `baseline_cnn.py` | 基线：Manual-CNN |
| `baseline_crnn.py` | 基线：Manual-CRNN（CNN+GRU） |
| `baseline_mobilenet.py` | 基线：MobileNetV2 0.5× |
| `baseline_random_search.py` | 搜索方法基线：随机搜索 |
| `dataset.py` | 数据集类（PNG→张量 + SpecAugment） |
| `net_builder.py` | 基因组 → PyTorch 网络 |
| `nsga2_eda.py` | NAS 主循环（NSGA-II+EDA+Surrogate，含消融） |
| `preprocess.py` | 数据预处理（mean/std + val/test 划分） |
| `project_status.py` | 本进度报告生成器 |
| `retrain_pareto.py` | Pareto 架构完整复训 |
| `search_space.py` | NAS 搜索空间（16 维编码） |
| `surrogate.py` | 代理评估（proxy + 随机森林） |
